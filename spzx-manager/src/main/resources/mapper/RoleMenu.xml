<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.manager.mapper.RoleMenuMapper">

<!--    <select id="findSysRoleMenuByRoleId" resultType="long">-->
<!--        select menu_id from sys_role_menu where role_id = #{roleId} and is_deleted = 0 and is_half = 0-->
<!--    </select>-->


    <select id="RoleMenuByRoleId" resultType="long">


        select menu_id from sys_role_menu where role_id = #{id}
        and is_deleted = 0

    </select>

</mapper>